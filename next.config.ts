// Exports the Next.js compilation, routing, and deployment configuration options for the application.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {

};

export default nextConfig;
